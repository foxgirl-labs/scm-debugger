/*
 * Tag used for filtering log messages
 *
 * @readonly
 * @string
 */
export const TAG = 'FSD';

/**
 * Enum for method return/status codes
 *
 * @readonly
 * @enum		 @int
 */
export const code = Object.freeze({
	OK:    0,
	NOWIN: 9
});
